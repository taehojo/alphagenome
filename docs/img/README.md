Put the browser screenshot here as `browser.png`, then uncomment the image line
near the top of the repository README.

Suggested capture: the gene view with one gene expanded, window about 1400 px
wide, so the variant table and the position view are both visible.
